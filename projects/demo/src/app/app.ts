import { Component, signal } from '@angular/core'
import { NgxContextmenu } from '../../../ngx-contextmenu/src/public-api'

@Component({
  selector: 'app-root',
  imports: [
    NgxContextmenu
  ],
  templateUrl: './app.html',
  styleUrl: './app.sass'
})
export class App {
  
}
